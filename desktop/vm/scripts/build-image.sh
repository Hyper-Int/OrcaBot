#!/usr/bin/env sh
set -eu

# TODO: build a minimal Linux VM image and inject the sandbox binary.
# This script should produce a versioned disk image for packaging.

printf '%s\n' "vm image build not implemented yet"
exit 1
