# OS
.DS_Store
Thumbs.db

# Secrets - NEVER commit these
.dev.vars
.dev.vars.*
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.p8
secrets.json
credentials.json
.watermark-lint.sh
desktop/.dev-secrets-encryption-key

# Rust/Tauri
desktop/app/src-tauri/target/

desktop/vm/image/

# Swift build artifacts (vz-helper)
desktop/app/src-tauri/vz-helper/.build/

# Workerd build temps and output
desktop/workerd/.tmp-build/
desktop/workerd/.tmp-frontend-build/
desktop/workerd/dist/

# Built/bundled resources (regenerated by build scripts)
desktop/app/src-tauri/resources/d1-shim/
desktop/app/src-tauri/resources/frontend/
desktop/app/src-tauri/resources/vm/
desktop/app/src-tauri/resources/workerd/

# Tauri frontend dist
desktop/app/dist/

# Go compiled binaries
sandbox/server
sandbox/mcp-bridge

# Node
node_modules/

# E2E test artifacts
e2e/test-results/
e2e/playwright-report/
e2e/node_modules/

# Claude Code settings
.claude/

# Local dev scratch / artifacts at repo root (anchored to root so they don't
# shadow tracked assets in subdirs, e.g. frontend/public/*.png, *.mp4).
# Screen recordings / scratch media
/*.mkv
/*.mov
/*.mp4
/*.webm
/*.png
# Scratch plans / docs
/PLAN-*.md
/EGRESS_HARDENING.md
/GMAIL.md
# Local logs / scratch output
/logs/
/sandboxlogs.txt
/reference/
# Local-only scripts and root node scratch
/deploy_dev.sh
/package.json
/package-lock.json

# Planning docs — keep locally, never publish to the repo
/PLAN-*